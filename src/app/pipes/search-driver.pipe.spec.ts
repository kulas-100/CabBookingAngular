import { SearchDriverPipe } from './search-driver.pipe';

describe('SearchDriverPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchDriverPipe();
    expect(pipe).toBeTruthy();
  });
});
