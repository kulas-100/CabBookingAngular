import { SearchCabPipe } from './search-cab.pipe';

describe('SearchCabPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCabPipe();
    expect(pipe).toBeTruthy();
  });
});
